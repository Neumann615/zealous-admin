# 更新日志

## 2026-09-24

- 🛡️ 登录防爆破：用户名/IP 双维度持久化限流，失败阈值触发服务端滑块验证码，连续失败自动锁定；反向代理可通过 `TRUST_PROXY` 还原真实 IP
- 🛡️ 服务端滑块验证码：挑战与答案由服务端持有，校验拖拽位置、时长与轨迹；一次性凭据绑定用户名/IP 并随登录提交
- 🛡️ 权限审计：服务端 403 决策写入监控日志，记录用户、路由、方法、所需权限、IP 与 UA，可在监控中心检索
- 🔐 会话生命周期加固：禁用账号拒绝登录，令牌按管理员 ID 绑定身份，登出 / 改密 / 禁用立即吊销存量令牌
- 🧭 数据库驱动权限：菜单类型支持目录 / 页面 / 按钮，权限标识字典化管理；服务端接口强制鉴权，前端按钮级隐藏与状态禁用，菜单支持拖拽排序
- ✨ 元数据管理补齐：编码项行内启停、批量导入、父级清空回根级、引用表单删除保护与分页筛选修复
- ✨ 表单生命周期：新增版本表、稳定业务键、草稿 / 发布 / 退役 / 恢复状态机与乐观锁，历史提交关联提交时版本；表单删除改为软删除并保护填写数据
- ✨ 表单数据版本化：数据页按版本筛选与导出，动态列跟随所选版本 Schema，详情抽屉按提交时版本回显；CSV 导出改为分页聚合并增加公式注入防护
- ✨ 表单分类与检索：新增两级分类管理、启停与删除约束，列表支持名称 / FormKey、状态、分类树筛选，父分类包含子分类结果
- 🛡️ 表单宽松契约：服务端校验 JSON 树、节点 id 与字段路径，保留设计器扩展配置；发布前要求存在可提交字段，提交仅兜底校验必填与基础值形态
- 🐛 统一业务错误响应：HTTP 状态与响应体 `code` 保持一致，避免客户端误判 401/403/409/428
- 🧪 测试矩阵扩展：单元测试 46 文件 / 531 用例、Playwright 34 条链路全绿，新增权限、元数据、登录安全、系统管理与表单生命周期 / 分类单测覆盖
- 📝 文档与 CI：公开 superpowers 规格索引，新增文档 / CHANGELOG 同步校验；CI 升级为 lint、前后端类型检查、单测、应用 / 服务 / 文档构建与全量 E2E，并上传 Playwright 报告

## 2026-09-16

- ✨ 字段级栅格：字段自带 `span` 与响应式断点，渲染时包一层 `Col`，画布外壳复用同一份换算且只镜像 `span`；属性面板新增「布局」分组
- ✨ 校验规则扩展：`ValidateRule` 从 5 种扩到 16 种，新增规则级 `trigger`，切类型时归一化丢弃无关字段
- ✨ 自定义校验复用公共事件表：`validator` 规则可内联正文或按名引用 `events.custom`，约定返回值语义；引用被删除时面板红字提示
- ✨ 声明式数据来源：`static` / `dict` / `api` 三类来源，接口只接受宿主注册名（`registerFormDataApis`，包不发起请求），`{{名路径}}` 插值 + 显式 `watch` 依赖重跑 + 防抖，竞态后写胜与取消、失败保留旧选项并提示、`AbortError` 静默
- 🔧 `ctx.reload` 落地：无参重取全部数据源字段、带参只重取命中字段，`beforeLoadData` / `afterLoadData` / `onReload` 三场景接线（只有手动重取触发 `onReload`）
- ✨ 字段联动：`control` 规则按值控制隐藏 / 禁用 / 必填，五种比较方式、效果取「或」，容器 `disabled` 下发到子字段；属性面板新增「联动」分组
- 🛡️ 形状校验收口：栅格、校验规则、数据来源、命名数据源表与联动规则统一走 `validateFieldRules`，保存 / 导出与解析同一口径
- 🐛 钩子改值重算联动：`ctx.setValue` / `setValues` 写值后立刻重算联动有效态；数据源挂载取数不再因宿主传入新的 schema 引用而重复请求；`hidden` 与 `required` 同时命中会面板红字提示；校准 `ctx.reload` / `await`、隐藏字段是否进报文、行内字段 reload 命中口径等文档措辞
- 🐛 数据来源面板修正：参数名输入框不再丢焦点、切到静态选项时用组件属性里的选项播种初值、`api` 切换保留 `parse`、数据来源按组件能力位（`optionProp`）只对下拉 / 单选 / 多选开放、`static` 不再触发请求类场景钩子、字典与接口的选项归一化合并并跳过无效项
- 🐛 数组行内字段名路径：面板不再列出行内子字段（`items.title` 恒为 `undefined`），联动的依赖字段与数据源的 `watch` 对解析不了的路径红字提示，并注明要手写 `items.0.title`
- ✅ 验证：27 文件 413 用例全绿，lint 0 error / 4 warning（2 条既有、2 条为新增规则列表编辑器同类），文档站构建通过，文档示例经 `parseSchema` 校验
- 📝 文档：新增「渲染项配置」页，Schema / 设计器 / 事件钩子三篇同步，设计规格补 §12.10

## 2026-09-15

- ✨ 表单填写数据落库：新增 `za_form_data` 表（JSON 主存 + 提交人 / 版本 / 状态冗余列），提供提交、分页查询、作废恢复、删除接口，删表单级联清数据，列表页显示数据量
- ✨ 新增表单数据管理页：按 schema 生成动态列、只读详情回显、CSV 导出
- 🔧 渲染页提交即落库：`FormRenderer` 新增外部表单实例 prop，提交后自动清空
- 🛡️ 字段名校验：按命名作用域校验唯一性（普通容器不产生新作用域）、禁止空值与空白与点号，属性面板内联提示、保存前拦截，导入带字段名问题的 schema 会被拒绝并说明冲突项
- 🔧 schema 升到 v2：新增 `events` / `dataSources` 可选段，解析统一收口到 `parseSchema()`（v1 自动迁移、更高版本拒绝、形状校验），导入与装载不再各自 `JSON.parse`
- 🔧 全局配置成体系：antd 透传白名单（布局 / 标签对齐 / 尺寸 / 冒号 / 禁用，类型级断言防漏键）+ 标签宽度、隐藏必填星号、提交 / 重置按钮开关，换算画布与运行时共用
- ✨ 事件钩子：12 个表单级场景 + 命名公共事件表，钩子只写函数体、结构化信封存储、记忆化编译；关键场景可中断提交 / 加载，公共事件用 `{ hook: '名字' }` 引用并可 `await ctx.emit(...)` 复用
- ✨ 渲染器场景接线：挂载 / 卸载 / 字段变化 / 提交前后 / 校验失败 / 重置逐场景触发，`ctx` 提供值快照与实时取值、改值、查字段、提示等能力
- ✨ 设计器「表单」页签拆为 表单配置 / 全局事件 / 公共事件 三段，钩子编辑红字提示与保存拦截同一口径（形状 + 语法 + 20000 字符上限）
- 🛡️ 事件加固：保存与解析共用 events 校验、`emit` 递归上限 5 层并去重提示、清空引用保留 `watch` / `order`、提交取值只带已注册字段
- ✅ 验证：18 文件 243 用例全绿，lint 0 error，文档站构建通过
- 📝 文档：新增「事件钩子」页，Schema 与设计器文档同步，设计规格补 §12.9

## 2026-09-11

- 📝 表单设计器文档对齐：P1–P4 实现计划复选框回填，设计规格新增「实现偏差与落地状态」章节，回写接口形态、组件数与后端技术栈差异

## 2026-09-10

- ✨ 表单设计器 P5：新增上传 / 金额输入 / 图标选择器与子表单 / 表格子表单 / 分步表单 6 个组件，内置组件达 37 个
- ✨ 渲染器支持嵌套名路径：子表单提交 `{ 字段名: { 子字段 } }`，表格子表单走 `Form.List` 提交数组并可增删行
- ✨ 表单发布 / 下线：列表操作列按 `status` 切换发布态
- 🔧 画布布局优化：画布撑满容器宽度，空态落点撑满整块画布
- 🔧 删除含子字段的容器需二次确认，确认删除后仍可撤销
- 🐛 设计页保存失败兜底：本地 schema 保留，可直接重试
- 🔧 服务默认端口调整：3001 → 3508
- 📝 文档站新增表单设计器章节：总览 / Schema 结构 / 组件清单 / 设计器与渲染器

## 2026-09-07

- ✨ 新增 form-designer 包：React + antd 6 自研低代码表单方案，声明式组件注册表，设计器画布与运行时渲染共用同一份 Schema
- ✨ 设计器三栏骨架：拖拽搭建、撤销 / 重做、导入导出 JSON、实时预览，Delete / Ctrl+Z / Ctrl+D 快捷键
- ✨ 31 个内置组件：17 基础输入 + 5 辅助 + 9 布局，右侧属性面板支持校验规则与全局表单配置
- ✨ 表单存取闭环：新增 `za_form` 表与 5 个接口，表单管理 / 设计 / 渲染三个演示页打通

## 2026-08-26

- ✨ 全局快捷键落地：Ctrl+K 唤起搜索、Ctrl+I 查看系统信息、Alt 组合键切换 / 关闭标签页与最大化
- ✨ 新增系统信息弹窗：展示浏览器 / 系统 / 分辨率 / 视口等信息
- ✨ 配置面板工具栏拖拽排序：卡片底部新增真实样式预览，拖拽调整 `toolbarOrder`
- 🔧 快捷键弹窗改两列展示，工具栏按配置顺序渲染替代硬编码

## 2026-08-19

- 🔧 标签栏拖拽库迁移：`@hello-pangea/dnd` 替换为 `@dnd-kit/react`，排序改为插入语义
- 🔧 依赖变更：移除 `@hello-pangea/dnd`，antd 升级至 6.6.1
- 📝 文档同步更新：拖拽排序相关描述改用 `@dnd-kit/react`

## 2026-08-15

- 📝 文档站同步更新：CHANGELOG 补全 07-30~08-14 记录，同步国际化、水印 store、i18n 配置、顶栏定位等新特性文档

## 2026-08-14

- ✨ 新增 locales 包统一国际化文案：layout 与示例应用全量中英文案统一从 `@zealous-admin/locales` 读取
- ✨ components 包接入国际化：新增 `ZaConfigProvider` Context 统一注入，主组件与演示页文案全部 `useT` 动态翻译
- 🔧 配置面板国际化：选项列表改用 `labelKey` 动态翻译，i18n 配置收拢至 `topBar.toolbar.i18n`
- 🐛 修复 antd 组件语言不生效：新增 `useAntdLocale` hook 统一注入 antd 语言包，嵌套 ConfigProvider 不再重置为英文
- 🐛 修复切换语言菜单不刷新：antd Menu 加 `key` 强制重建，子菜单数据兜底刷新
- 🔧 升级 antd 至 6.6.0
- ✨ 新增反馈弹窗：modern-screenshot 全屏截图预览 + 富文本描述编辑 + 模拟提交

## 2026-08-13

- ✨ 新增国际化功能：`useI18nStore` 集中管理语言状态并持久化，antd locale 动态懒加载，菜单/标签页/面包屑按语言解析，工具栏语言切换器，layout 内置中英文案替换硬编码
- ✨ 示例应用接入国际化：前端统一维护菜单多语言映射，通过 `messages` prop 注入 `LayoutProvider`

## 2026-08-12

- ✨ 新增快捷键弹窗：两列网格展示全局/主导航/标签栏/页面快捷键

## 2026-08-11

- ✨ 新增个人信息弹窗：个人资料展示 + 修改密码功能
- 🔧 修改密码接口规范化：响应改为标准 `success/failed` 格式，身份改用认证 token 而非信任 body

## 2026-08-09

- 📝 文档同步更新：组件数量更新为 12 个，新增 `ZaSignaturePad` 文档页与侧边栏入口

## 2026-08-07

- ✨ 新增富文本编辑器组件：基于 Quill 2 的轻量级编辑器，支持受控/非受控、自定义工具栏、只读模式
- ✨ 新增 Iframe 演示组件、签名板组件、路由传参示例
- 🔧 Demo 边框统一去除、风格实验室页面重构

## 2026-08-05

- ✨ 动态标题接入配置项：`isEnableDynamicTitle` 开关控制标题是否随路由动态更新
- ✨ 水印配置本地缓存：zustand persist 持久化水印配置

## 2026-08-04

- ✨ 新增水印功能：antd Watermark 按配置包裹布局，文案自动追加当前用户名
- 🔧 依赖升级、菜单边框条件化

## 2026-08-03

- 🔧 搜索框美化、配置面板三列布局、页面过渡动画精简
- ✨ 居中布局演示页面

## 2026-08-02

- ✨ 顶部定位模式：`topBar.position` 支持 `static`/`fixed`/`sticky` 三种模式
- 🐛 KeepAlive 刷新修复：缓存页按路径记录刷新版本号，普通切换不影响缓存状态
- 🔧 TabBar 宽度模式适配：`fixed`/`auto`/`auto-min`/`auto-max` 四种宽度模式

## 2026-08-01

- 🔧 TabBar 多风格适配：`default` / `card` / `block` 三种标签样式

## 2026-07-30

- 🔧 TabBar 图标显示配置化与激活态支持
- 🔧 仓库统计排除 AI 辅助文件

## 2026-07-29

- 🔧 面包屑导航优化：根据路径前缀关系自动判断追加或替换模式
- 🔧 TabBar 图标动态查找：根据路径查找菜单图标，支持父节点继承
- 📝 文档站完善：新增 utils 工具函数文档（5 个模块），新增 ZaIframe 组件文档

## 2026-07-28

- ✨ 新增 utils 工具包：data/env/time/file/parse 五大模块通用工具函数
- ✨ 新增 Iframe 外链嵌入组件：`ZaIframe` 通用 iframe 组件（自动撑满+加载状态）
- 🔧 菜单图标取消必填校验
- ♻️ 代码重构使用 utils 工具函数：替换手写深拷贝/分组/排序逻辑

## 2026-07-27

- 🔧 背景色全站统一：`colorBgLayout` → `colorBgBase` + 边框
- 🔧 首页重设计：BorderBeam 欢迎卡片、功能特性卡片、应用场景卡片
- ✨ 驼峰转换工具：snake_case → camelCase 键名转换
- 🔧 彩带庆祝效果重构：分阶段空气阻力物理模型
- 🔧 菜单滚动条隐藏：内容可滚动但不显示滚动条

## 2026-07-26

- 🔧 系统管理页面标准化：Form.useForm + rules 验证、主题化样式
- 🔧 Dashboard 代码重构：提取 shared hooks/data/styles，消除 ~80% 重复
- 🔧 登录页全面重写：token 化样式、错误反馈、响应式卡片
- ✨ MCP StreamableHTTP 服务：地理位置和天气查询工具

## 2026-07-25

- ✨ Ant Design 全组件展示页：覆盖 76 个 antd 6.x 组件
- ✨ 多主题类型系统：支持 8 种主题一键切换
- ✨ 主题类型选择器与权限控制
- 🔧 废弃 API 迁移：`maskClosable` → `mask={{ closable }}`

## 2026-07-24

- ✨ 页面 KeepAlive 缓存：双层渲染架构，缓存页保持状态
- ✨ 缓存配置外部化：新增 `cachedPages` prop
- 🔧 HTTP/用户/权限基础设施集成至 layout
- ✨ 401 过期模式分流与重新登录弹窗
- ✨ 新增 3 套主题：cartoon/hacker/shadcn

## 2026-07-23

- ✨ 金融可视化大屏 dashboard3：Three.js 3D 中国地图、ECharts 图表
- ✨ 大屏演示2 3D 中国地图：Three.js 实现 GDP 热力图、省份下钻
- 🔧 大屏图表优化：数据刷新间隔调整、饼图重构

## 2026-07-22

- ✨ 菜单激活图标：选中时切换 activeIcon，父节点链路级联
- ✨ 菜单 path 根据 name 层级自动计算，修改时级联更新
- ✨ 首页美化：顶部加 Logo + 项目名称
- 🔧 导航管理：新增前端名称列，父节点隐藏 path

## 2026-07-21

- 🗑️ Mall 模块全面清除（73 个文件，~15000 行）
- ✨ 用户管理、角色管理、导航管理、字典管理完整 CRUD
- 🔧 Service 迁移至 Express + node:sqlite，统一驼峰映射与日期格式
- 🔧 za_menu 新增 active_icon 字段，种子数据递归插入优化
- 🔧 导航管理改为树表格展示，默认全部展开
- 🔧 去除 Ums/useMall 前缀，统一命名规范

## 2026-07-20

- ✨ 新增移动端响应式布局，窄屏自动切换 Drawer 抽屉菜单
- 🔧 移动端汉堡图标移至工具栏，面包屑自动隐藏

## 2026-07-19

- ✨ 新增 AppMessageProvider，统一注入 antd message/modal
- 🗑️ 删除 AppMessageInit 组件

## 2026-07-18

- 🔧 组件命名统一改为 Za 前缀
- ✨ 新增 MobileBlock 移动端访问控制

## 2026-07-17

- 🗑️ 移除快乐模式和紧凑模式
- ✨ 工具栏新增页面刷新

## 2026-07-15

- ✨ 动态网站标题、菜单图标悬停放大

## 2026-07-14

- ✨ 新增全局路由搜索功能

## 2026-07-13

- 📝 README 与文档全面整理
- 🐛 修复 layout-config、layout-modes 类型错误

## 2026-07-12

- ✨ 布局居中显示（outside/inside 两种作用范围）
- 🐛 修复 ConfigPanel 开关绑定错误、数据污染
- 🔧 全局背景色统一，Demo 页面样式规范化

## 2026-07-11

- 🐛 修复风格实验室随机数据拼写错误

## 2026-07-10

- ✨ ConfigPanel 新增复制配置功能
- 🔧 工具栏配置简化、SliderCaptcha 样式优化

## 2026-07-09

- 🔧 Header 动态渲染、Toolbar 按钮可配置化
- 🔧 上级菜单选择改为树形组件

## 2026-07-08

- 🔧 清理调试日志
- 🐛 TabBar 标签关闭交互优化

## 2026-07-06

- 🐛 浏览器后退路由同步修复
- ✨ 菜单分配弹窗化、UserInfo 信息完善
- 🔧 全局 message/modal 主题同步

## 2026-07-05

- ✨ 菜单弹窗化、图标系统重构
- 🔧 菜单全后端驱动

## 2026-07-04

- 🔧 面包屑导航重构
- 🎨 19 个页面搜索表单 UI 统一
- 🔄 项目重命名为 zealous-admin

## 2026-07-03

- ✨ 新增后端服务、PatternBg 组件
- 📦 配置文件迁移至各 packages

## 2026-07-02

- 🔧 Header 重构（819→21 行），新增 TabBar/Breadcrumb/ConfigPanel

## 2026-07-01

- ✨ 新增风格实验室、useLayoutSetting Hook
- 🎨 首页重构、Tailwind CSS 集成

## 2026-06-30

- ✨ 新增 Logo 组件，重构加载页面

## 2026-06-29

- 🎨 全项目格式化，ESLint 升级 v10

## 2026-06-26

- ✨ 新增 IconPicker（32+ 图标库）

## 2026-06-25

- ✨ 新增 UserInfo 用户信息组件

## 2026-06-24

- ✨ 新增 ShinyText 流光文字组件

## 2026-06-23

- ✨ 新增 LinkPreview、SliderCaptcha 组件

## 2026-06-14

- 🔧 创建 AppLayout 组件，职责分离

## 2026-06-12

- ✨ 暗色模式跟随系统自动切换

## 2024-03-10

- ✨ 项目初始化，布局/主题/工具栏/登录/首页等核心模块
