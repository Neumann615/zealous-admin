# 复制为 service/.env 填写；生产环境请通过部署平台注入同名环境变量
# 令牌签名密钥，缺失时生产环境拒绝启动
JWT_SECRET=change-me-to-a-long-random-string
# 访问令牌有效期，默认 2h（前端会静默续期）
# JWT_EXPIRES_IN=2h
# 反向代理部署时配置（loopback / 1 / CIDR），登录限流按真实客户端 IP 统计
# TRUST_PROXY=loopback
